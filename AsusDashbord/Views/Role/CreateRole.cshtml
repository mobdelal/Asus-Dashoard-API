@model IdentityRole<int>

<form asp-controller="Role" asp-action="CreateRole" method="post">
 
    <div class="form-group">
        <input asp-for="Name" class="form-input" placeholder="Enter Role" />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>
    <div class="form-group">
        <button type="submit" class="form-submit btn btn-outline-success">Submit</button>
    </div>
</form>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}